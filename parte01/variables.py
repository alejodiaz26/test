### Now DataType of the variable.

### In python the Literals create a object with the class that correspond to the dtype. in this case built in classes (int, float, str, list, dict, tuple, set, etc...)

name = "Alejandro"
age = 24
print(type(name), type(age)) ### name Type --> str (String) & age Type --> int (Integer)


### See the location of the variable in the Memory:

print(f"Location in memory: {id(age)}")  ### Location in memory 

### Each variablle have an unique identifier if the value does not the same.



